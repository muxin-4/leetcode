export default (str) => {
	// 建立电话号码键盘映射
	let chars = ['abc', 'def', 'ghi', 'jkl', 'mno', 'pqrs', 'tuv', 'wxyz'];
	// 把输入字符串单字符分隔变成数组，234 => [2, 3, 4]，保存键盘映射后的字母内容，如 23 => ['abc', 'def']
	let code = []
	// str.split('').forEach();
	console.log('split', str.split);
}
